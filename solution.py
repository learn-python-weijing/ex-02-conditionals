# Exercise 02 — Conditionals & Control Flow
# Fill in each function. Do NOT change the function names or parameters.


def classify_score(score):
    """Return a grade based on the score:
    90-100 -> "A"
    75-89  -> "B"
    60-74  -> "C"
    below 60 -> "F"
    """
    pass


def fizzbuzz(n):
    """Classic FizzBuzz for a single number n.
    Return "FizzBuzz" if divisible by both 3 and 5,
    "Fizz" if divisible by 3 only,
    "Buzz" if divisible by 5 only,
    otherwise return the number as a string.
    """
    pass


def can_vote(age, is_citizen):
    """Return True if the person can vote.
    They must be 18 or older AND a citizen.
    """
    pass


def absolute_value(n):
    """Return the absolute value of n WITHOUT using abs()."""
    pass


def largest(a, b, c):
    """Return the largest of three numbers."""
    pass
